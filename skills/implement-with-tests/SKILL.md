---
name: implement-with-tests
description: Make the smallest scoped code change and rely on deterministic verification. Use for implementing or correcting bugs, features, refactors, and tests in an isolated worktree.
---

# Implement with tests

Work only inside the assigned worktree. Follow the diagnosed plan and protected contracts, keep the diff scoped, add or update the narrowest regression test, and never claim success from model judgment. The orchestrator's approved deterministic commands decide whether the change passes.
